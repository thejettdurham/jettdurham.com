box: debian

build:
  steps:
    - arjen/hugo-build@1.8.1:
        version: "0.15"

deploy:
  steps:
    - schickling/s3-website:
        key: $KEY
        secret: $SECRET
        bucket: $BUCKET
        region: $REGION
